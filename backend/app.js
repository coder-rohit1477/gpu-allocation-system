'use strict';

// Entry point — boots the HTTP server.
// Nothing else should require() this file.
// Code that needs the Express app (tests, utilities) should require('./server/app') directly.
require('./server');
